import { HiDatabase, HiDesktopComputer, HiSearch, HiServer, HiSwitchHorizontal, HiTerminal } from 'react-icons/hi';

export const categoryIconMap = {
    STORAGE: HiDatabase,
    ESX: HiServer,
    VM: HiDesktopComputer,
    VC: HiSwitchHorizontal,
    PWWN: HiTerminal,
};

export const fallbackCategoryIcon = HiSearch;
